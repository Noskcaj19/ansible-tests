# Do not edit directly. Managed by Ansible
[libdefaults]
 default_realm = {{ base_server_krb_realm }}
 dns_lookup_realm = false
 dns_lookup_kdc = true
 rdns = false
 ticket_lifetime = 24h
 forwardable = true
 allow_weak_crypto = true

#[realm]
# not needed because of autoconfig
# {{ base_server_krb_realm }} = {
#   ...
# }

# not needed because of vasd conf
#[domain_realm]
# .{{ base_server_krb_realm | lower }} = {{ base_server_krb_realm }}
# {{ base_server_krb_realm | lower }} = {{ base_server_krb_realm }}
